name: Launch Scala Steward

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  scala-steward:
    name: Launch Scala Steward
    runs-on: ubuntu-20.04
    steps:
      - name: Launch Scala Steward
        uses: scala-steward-org/scala-steward-action@v2
